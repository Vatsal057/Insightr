package com.example.vault.theme

import androidx.compose.ui.graphics.Color

// ─── Core Palette ────────────────────────────────────────────────────────────
val VaultBackground     = Color(0xFF08080C)
val VaultSurfaceL1      = Color(0xFF0F0F15)
val VaultSurfaceL2      = Color(0xFF141420)
val VaultSurfaceL3      = Color(0xFF1A1A2E)

// ─── Accent ──────────────────────────────────────────────────────────────────
val VaultAccent         = Color(0xFFA78BFA)   // soft violet
val VaultAccentMuted    = Color(0x33A78BFA)
val VaultAccentDim      = Color(0x1AA78BFA)
val VaultAccentGlow     = Color(0x66A78BFA)

// ─── Text ────────────────────────────────────────────────────────────────────
val VaultTextPrimary    = Color(0xFFF0F0F5)
val VaultTextSecondary  = Color(0xFF8B8B9E)
val VaultTextTertiary   = Color(0xFF55555E)

// ─── Borders & Dividers ──────────────────────────────────────────────────────
val VaultBorder         = Color(0x0AFFFFFF)
val VaultBorderStrong   = Color(0x18FFFFFF)
val VaultDivider        = Color(0x08FFFFFF)

// ─── Semantic Status ─────────────────────────────────────────────────────────
val VaultFact           = Color(0xFF34D399)   // emerald  — verified facts
val VaultOpinion        = Color(0xFF60A5FA)   // blue     — opinions
val VaultUnverified     = Color(0xFFF59E0B)   // amber    — unverified claims
val VaultError          = Color(0xFFEF4444)

// ─── Concept Type Colors ─────────────────────────────────────────────────────
val VaultConceptColor   = Color(0xFFA78BFA)   // violet  — concept
val VaultFrameworkColor = Color(0xFF60A5FA)   // blue    — framework
val VaultToolColor      = Color(0xFF34D399)   // emerald — tool
val VaultBookColor      = Color(0xFFFBBF24)   // amber   — book
val VaultPersonColor    = Color(0xFFF472B6)   // pink    — person
val VaultMethodColor    = Color(0xFF818CF8)   // indigo  — methodology
val VaultWebsiteColor   = Color(0xFF2DD4BF)   // teal    — website

// ─── Daily Discovery Warm Glow ────────────────────────────────────────────────
val VaultWarmAmber      = Color(0xFFF59E0B)
val VaultWarmGlow       = Color(0x26F59E0B)
