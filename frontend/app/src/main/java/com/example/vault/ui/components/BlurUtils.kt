package com.example.vault.ui.components

// BlurUtils.kt — kept as documentation; actual blur is applied directly in GlassCard
// via Modifier.blur() which is the correct Compose API (no sealed class extension needed).
// This file is intentionally minimal.
